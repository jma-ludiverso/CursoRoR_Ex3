<tr class="items">
	<td><%= f.collection_select :product_id, Product.all, :id, :product_with_category, { prompt: true }, { class: 'selectproduct' } %></td>
	<td class="centercell"><%= f.text_field :quantity %></td>
  	<td class="centercell">
  		<%= f.hidden_field :id %>
  		<%= f.hidden_field :_destroy %>
  		<%= link_to_function "Remove", "remove_line_item(this)" %>	
	</td>
</tr> 